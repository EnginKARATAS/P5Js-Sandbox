let timer = 5
var sorular = [
	soru1:"selam","vedad"
];
function setup() { 
  createCanvas(400, 400);
} 

function draw() { 
	
  background(244);
  textSize(100);
  text(timer, width/2, height/2);
   
}